---
import "../styles/main.css";
import Navbar from "../components/navbar.astro";
import Footer from "../components/footer.astro";
const year = new Date().getFullYear();
const base = import.meta.env.BASE_URL;
---

<Navbar />
<main>
  <section class="hero container">
    <h1 class="hero-title">Your backend from a config file</h1>
    <p class="hero-tagline">
      Describe entities, views, and access rules in YAML. We serve a production
      REST API with RBAC, row-level access, SQL views, and event history.
      <strong>Bring your own frontend. </strong>
    </p>
    <p class="hero-actions">
      <a role="button" class="outline" href={`${base}docs/quick-start`}>
        How it works
      </a>
    </p>
  </section>
</main>
<Footer />
